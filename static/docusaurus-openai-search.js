// This script injects the OpenAI API key into the window object
// Replace ${OPENAI_API_KEY} with your actual key in production
// or set it as an environment variable in your build process
window.OPENAI_API_KEY = '${OPENAI_API_KEY}'; 